export const metadata = {
    title: 'Services',
    description: 'Comprehensive career support services including resume writing, LinkedIn optimization, job marketing, interview preparation, and end-to-end placement support.',
};

export default function ServicesLayout({ children }) {
    return children;
}
